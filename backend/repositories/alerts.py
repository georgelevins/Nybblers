# TODO: implement when schema is final
# Use database.get_pool() for connections.

# async def create_alert(user_email: str, query: str, query_embedding: list[float] | None) -> AlertCreateResponse:
#     """INSERT INTO alerts (user_email, query, query_embedding, ...). Embed query first."""
#     ...
