{
  "name": "nybblers-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "pip install -r requirements.txt || pip install --break-system-packages -r requirements.txt",
    "start": "python3 -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
